# Set the architecture depending on the target:
CARCH="aarch64"  # For Orange Pi Zero 3
# CARCH="armv7h"  # For BeagleBone Green
# CARCH="x86_64"  # For AMD64

# Corresponding CHOST variables for each architecture:
CHOST="aarch64-linux-gnu"  # For Orange Pi Zero 3
# CHOST="armv7l-linux-gnueabihf"  # For BeagleBone Green (ARMv7)
# CHOST="x86_64-pc-linux-gnu"  # For AMD64

SRCEXT='.src.tar.gz'
PKGEXT='.pkg.tar.zst'
