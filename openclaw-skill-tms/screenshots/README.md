# Screenshots for ClawHub

Place 3–5 PNG screenshots here for the ClawHub listing (1920×1080 or 1280×720). Suggested content:

1. **Hero** — OpenClaw chat: user asks to add a download or list downloads; agent response with result.
2. **Config** — Fragment of `openclaw.json` showing `skills.entries.tms` with `TMS_API_URL` and `TMS_API_KEY`.
3. **Output** — Example of list/download status or search results in chat.
4. **Optional** — TMS Swagger UI (`{TMS_API_URL}/api/v1/docs`) or API response.

When publishing, attach these files to the skill listing on ClawHub.
